# Project Notes

- Do not run npm checks, builds, lint, or test commands unless the user explicitly asks for them.
- Current UI direction: keep the prompt library as a simple grid. Each card should show only the image and a `Copy prompt` action.
